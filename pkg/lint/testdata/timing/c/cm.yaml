---
apiVersion: v1
kind: ConfigMap
metadata:
  name: other
  namespace: default
data:
  value: "x"
  # shell, not Flux substitution: c has no postBuild, so this is applied verbatim
  script: echo "${HOME}"
